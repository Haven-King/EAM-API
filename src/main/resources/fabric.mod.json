{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${libname} version ${libver}",
  "icon": "assets/eam/icon.png",
  "description": "A minimal API to make creating Entity Attribute Modifiers easier",
  "license": "MIT",
  "contact": {
    "sources": "https://github.com/${repo}"
  },
  "environment": "*",
  "entrypoints": {
  },
  "mixins": [
    "mixins.${libname}.json"
  ],
  "depends": {
    "fabricloader": ">=0.4.0"
  },
  "custom": {
    "modmenu:api": true
  }
}
